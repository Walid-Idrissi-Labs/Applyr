name: Keep Render backend warm

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check endpoint
        run: curl -sS -o /dev/null -w "HTTP %{http_code}\n" --fail https://applyr-1abi.onrender.com/up
